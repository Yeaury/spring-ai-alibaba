spring:
  application:
    name: cfunction-calling-example

  ai:
    dashscope:
      api-key: ${AI_DASHSCOPE_API_KEY}
    alibaba:
      plugin:
        weather:
          api-key: ${AI_WEATHER_API_KEY}

